---
name: command-code-delegate
description: 범위가 명확한 단순 코딩, 반복 수정, 기계적 변환을 Command Code CLI의 DeepSeek에 위임한다. 작업자가 자체 검증과 수정을 마친 최종 결과를 메인 모델이 독립 검증하는 구현 위임 절차. 기획·중요 설계 판단·최종 리뷰는 메인 모델이 담당한다.
---

# Command Code 구현 위임

## 역할과 실행 설정

- 메인 모델은 목적·요구사항·설계·수정 범위·완료 기준·검증 방법을 정한다. 사용자가 선택한 메인 모델을 유지한다.
- 비대화형 `cmd -p`에 `--model deepseek/deepseek-v4.1-flash --effort high --yolo --output-format json`과 작업에 맞는 `--max-turns`를 명시한다.
- Node.js 22 이상에서 실행한다. 디렉터리별 설정이 Node 20 등을 선택하면 설치된 Node 22 이상 런타임의 bin 디렉터리를 해당 호출의 PATH 앞에 지정한다.
- YOLO 모드는 사용자가 지속적으로 요청한 기본 실행 방식이며 매번 허락을 다시 묻지 않는다. 이는 맡긴 작업 범위 안의 실행에 적용되며 관련 없는 변경이나 외부 게시·배포 권한을 추가하지 않는다.
- 병렬 편집 또는 사용자 변경과 충돌할 수 있으면 별도 worktree로 격리한다. 메인 모델은 작업자가 편집 중인 파일을 동시에 수정하지 않는다.

## 작업자에게 전달할 내용

외부 CLI는 현재 대화나 Codex 지침을 자동으로 공유하지 않는다. 다음을 명시적으로 전달한다.

- 작업 목적과 기대 동작, 수정 허용 범위와 제외 범위.
- 적용할 프로젝트 지침과 필요한 파일·컨텍스트. 비밀정보는 프롬프트에 넣지 않는다.
- 완료 기준과 변경에 맞는 테스트·타입 검사·린트 등 검증 방법.
- 아래 자체 완료 절차와 최종 인계 형식. 이 위임 지침을 재귀 적용해 다른 작업자를 호출하지 않고 직접 구현·검증하도록 명시한다.

## 작업자 자체 완료 절차

1. 구현 후 스스로 diff를 검토하고 요구사항 충족, 누락, 회귀 가능성을 확인한다.
2. 변경에 맞는 검증을 직접 실행한다. 실패나 결함이 있으면 스스로 수정하고 관련 검증을 다시 실행한다.
3. 완료 기준을 만족하고 해결 가능한 결함을 처리할 때까지 구현 → 검증 → 수정 → 재검증을 내부적으로 반복한다. 첫 구현 직후 메인 모델에 검사를 요청하거나 중간 결과를 최종 결과로 반환하지 않는다.
4. 최종 diff와 실제 검증 결과를 확인한 뒤에만 최종 결과를 인계한다. 통과한 동일 검증을 이유 없이 무한 반복하지 않는다.
5. 인증·사용 한도·모델 미지원·필수 정보 부족·외부 환경 장애 또는 턴 한도로 완료할 수 없으면 `BLOCKED` 또는 `INCOMPLETE`로 보고한다. 실행하지 못한 검증을 통과했다고 하지 않는다.

최종 응답에는 다음을 포함한다.

- 상태: `READY_FOR_REVIEW`, `BLOCKED`, `INCOMPLETE` 중 하나.
- 변경 내용과 수정 파일, 완료 기준 충족 여부.
- 실행한 검증 명령과 실제 결과, 수정 후 재검증 결과.
- 남은 제약·미해결 항목. 없으면 없다고 명시한다.

## 메인 모델 인수 검증

- 실행 중에는 진행 상태를 파악하되 중간 산출물을 최종 코드 리뷰 대상으로 삼지 않는다. 사용자의 방향 수정이나 범위 이탈 등 개입이 필요한 경우는 즉시 조정한다.
- JSON 출력은 이벤트 스트림과 마지막 결과 행으로 구성된다. 프로세스 종료만으로 성공을 판단하지 않고 최종 상태·종료 코드·검증 증거를 확인한다. 턴 한도 도달(exit 8)은 미완료다.
- `READY_FOR_REVIEW` 결과를 받은 뒤 실제 diff를 독립 검토하고 필요한 검증을 실행한다. 작업자의 완료 주장만으로 작업을 종료하지 않는다.
- 단순 후속 수정은 발견 사항과 수락 기준을 전달해 다시 DeepSeek에 맡긴다. 작업자는 동일한 자체 검증·수정 절차를 거친 뒤 최종 결과를 다시 인계한다.
- 도메인별 리뷰 스킬의 필수 리뷰 절차는 그대로 수행한다. 작업자의 자체 검증이 메인 모델의 최종 검증이나 필수 리뷰를 대체하지 않는다.
- 인증 실패·사용 한도·모델 미지원 등으로 위임할 수 없으면 원인을 알린다. 다른 유료 모델이나 추가 크레딧으로 자동 전환하지 않는다. 메인 모델이 구현을 대신해야 한다면 사용자에게 역할 변경을 확인한다.
